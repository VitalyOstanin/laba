// Test stub for SvelteKit's `$app/state` (see app-navigation.ts). Exposes a
// minimal `page` with a URL so components can read search params under vitest.
export const page = { url: new URL("http://localhost/") };
